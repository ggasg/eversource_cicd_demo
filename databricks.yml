# This is a Databricks asset bundle definition for databricks-local-dev.
# The Databricks extension requires databricks.yml configuration file.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.

bundle:
  name: databricks-local-dev

include:
  - resources/*.yml

variables:
  catalog_name:
    description: "Just my catalog name"
    default: "gguitarts_own_cat"
  schema_name:
    description: "Just my schema name"
    default: "home_iot"

targets:
  dev:
    mode: development
    default: true
    workspace:
      host: https://adb-984752964297111.11.azuredatabricks.net
      root_path: /Workspace/Users/gaston.guitart@databricks.com/Customer ASQs/eversource_cicd_demo/.bundle/${bundle.name}/${bundle.target}
      file_path: /Workspace/Users/gaston.guitart@databricks.com/Customer ASQs/eversource_cicd_demo

  ## Optionally, there could be 'staging' or 'prod' targets here.
  #
  # prod:
  #   workspace:
  #     host: https://adb-984752964297111.11.azuredatabricks.net
